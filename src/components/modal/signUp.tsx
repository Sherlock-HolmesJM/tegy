import { ModalWrapper } from "./base";

interface Props {}

function SignUp(props: Props) {
	const {} = props;

	return <ModalWrapper theme={window.theme} title="Sign-in"></ModalWrapper>;
}

export default SignUp;
