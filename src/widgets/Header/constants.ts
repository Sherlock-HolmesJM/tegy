import { LinkItemProps } from "./type";

export const headerClasses = {
  item: "header-item",
};

export const NavList: LinkItemProps[] = [];

// if (process.env.NODE_ENV === "development") {
//   NavList.push({ name: "Playground", href: { pathname: "/playground" } })
// }
